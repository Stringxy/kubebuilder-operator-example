---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: xydaemonsets.xytest.xy.io
spec:
  group: xytest.xy.io
  names:
    kind: XyDaemonset
    listKind: XyDaemonsetList
    plural: xydaemonsets
    singular: xydaemonset
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: XyDaemonset is the Schema for the xydaemonsets API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: XyDaemonsetSpec defines the desired state of XyDaemonset.
            properties:
              command:
                items:
                  type: string
                type: array
              image:
                type: string
              replicas:
                type: integer
            required:
            - command
            - image
            type: object
          status:
            description: XyDaemonsetStatus defines the observed state of XyDaemonset.
            properties:
              autoScalingStatus:
                type: string
              availableReplicas:
                type: integer
              podNames:
                items:
                  type: string
                type: array
            required:
            - autoScalingStatus
            - availableReplicas
            - podNames
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
